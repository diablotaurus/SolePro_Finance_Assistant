Правила:

0. Проект должен полностью соответствовать принципам MVC, OOP, DRY, SOLID!
1. Все файлы проекта сохранять в UTF-8, только чистый UTF-8 (никакого BOM, без BOM и прочего, чистый UTF-8)!
2. Прошу быть ОЧЕНЬ внимательным и аккуратным! Главное ничего не сломать!
3. Обязательно при коммите (commit) менять версию в pyproject.toml в строке version = ""
4. Не нужно создавать никакой новый код заранее, не проанализировав все файлы проекта (возможно весь необходимый код уже есть в других файлах проекта), если для выполнения задачи требуется дописать код, то сначала ты должен узнать существует ли этот код в других модулях и файлах, ты должен сообщить мне, что требуется изучить другие файлы из проекта.
5. Всегда проверяй вызовы из RunDesktopApp.pyw, src/solepro/presentation/telegram/bot.py и src/solepro/presentation/desktop/application.py.
6. Напоминаю, никаких изменений в код не вноси без согласования! Сначала узнай, потом предлагай правки! Это очень важно для меня.
7. Если для выполнения задачи необходимо создать новые функции, методы или классы, то обязательно нужно сначала их согласовать, и проверить на совместимость с остальной частью кода.
8. При коммите файл changelog называть строго по схеме: changelog_{dd.mm.YYYY}_{version}.html, где {version} — это значение версии из pyproject.toml (строка version = ""), а {dd.mm.YYYY} — дата коммита. Файл сохранять в каталог docs/changelog/. Образец оформления отчёта — docs/changelog/changelog_08.02.2026_1.html. Напоминание: согласно правилу 3 версия в pyproject.toml меняется при каждом коммите, поэтому имя changelog должно соответствовать новой версии.

!!! Итак, алгоритм такой: сначала анализируешь предоставленный код, затем предлагаешь изменения, если требуется код из других файлов проекта, запрашиваешь его для анализа, затем предлагаешь правки в код, я проверяю и утверждаю, и только потом создаешь готовые файлы.
